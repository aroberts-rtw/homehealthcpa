# Google Analytics 4 measurement ID for this site (separate from rtwadvisors.ai)
# Create a new GA4 property at analytics.google.com, then add this domain as a data stream.
# Copy the Measurement ID (G-XXXXXXXXXX) here, then add PUBLIC_GA4_ID in Vercel env vars.
PUBLIC_GA4_ID=TODO_GA4_ID

# Google Search Console HTML verification meta tag value
# Add homehealthcpa.com as a new property in Search Console, choose HTML tag method,
# copy the content="..." value here, then set PUBLIC_SC_VERIFICATION in Vercel env vars.
PUBLIC_SC_VERIFICATION=TODO_SC_VERIFICATION
